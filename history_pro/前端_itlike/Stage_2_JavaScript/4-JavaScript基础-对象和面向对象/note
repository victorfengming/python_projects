基本要求:
    定义一个构造函数

对象
    什么是对象
        生活中的对象
        对象具有特征和行为
    面向对象
        可以创建自定义的类型,很好的支持继承和多态.
        面向对象语言
            C++
            java
            C#
            python
        特征:
            封装,继承,和多态
        万物皆对象:
            世间的一切事物都可以用对象来描述
    基于对象
        无法创建自定义的类型,不能很好的支持继承和多态
        基于对象的语言,比如JavaScript
JavaScript中常见的对象
    无序属性的集合
        其属性可以包含基本值,对象或函数
        对象就是一组没有顺序的值
        我们可以吧JavaScript中的对象想象成键值对,其中值可以是数据和函数
    对象的行为和特征
        特征---属性
        行为---方法
    字面量对象
        无序属性的集合
        对象就是(键值键值键值键值键值键值键值)
    JSON
        什么是JSON?
            JSON是一种数据交换格式
            JavaScript对象表形式
        JSON和对象字面量的区别
            JSON的属性必须用双引号引起来,对象字面量可以省略键的引号
            JSON本质上是一种数据交换格式

                JSON有两种结构:对象 和 数组,两种结构相互组合
    遍历对象的属性
        for in 遍历对象的属性
    构造函数
        就是一个函数,(能够产生对象函数)
        new Object()
    this
        this所在的函数,在哪个对象当中,this就是谁
        谁调用this就是谁
        构造函数中的this,始终是new的当前对象
    构造器(constructor)和原型属性(prototype)
    其他语言中面向对象
数据类型和内存分析

JavaScript中的内置对象

