JavaScript中的内置对象
    什么是内置对象
    常见的内置对象
    Array高级API
        sort()升序排列数组项-即最小值位于最前面,最大值排在最后面
        slice()返回从原数组中的开始和结束位置的一个新数组
        splice()可以实现删除\插入\替换
        forEach()对数组进行遍历循环,对数组中的每一项运行给定函数
            格式:arr.forEach(function (value, index));
        map()
            映射,对数组中每一项运行给定函数,返回每次函数调用的结果组成的数组.
        filter()
            过滤功能,数组中每一项运行给定函数,返回满足过滤条件元素组成的数组
        every()
        some()
