REM
    概念：
        rem可以在用户浏览网页时，根据屏幕的尺寸，来向用户展示更适合用户的布局，文字，图片，按钮
        rem（font size of the element）时指相对于根元素的字体大小单位
        一旦根节点heml定义的font-size变化，那么整个网页中运用到rem的也会随之变化

    使用：
        重新计算那些被放大的字体rem数值，避免字体大小的重复声明
        目前PC端，开始普遍使用rem，移动端基本完全使用

    说白了就是用一个相对的单位，来代替像素


